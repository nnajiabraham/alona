Perfect, go ahead and start implementing @RFC-XXX as planned, don't forget to check off tasks from the list after completeing said task and also update the implementation notes summary section with a summary of changes made, files impacted, links etc.. as this will be used as context by other llms and agents to understand the changes made when implementing an rfc before proceeding to next step/task. Also note to use the @Makefile cmds for running any cmds to run, test etc.. the app. need to make sure app fits into the make cmd interface created from earlier RFC so make sure to always use the makefile cmds for running builds tests lint serve etc... 

##########VERSION 2##########
go ahead and start implementing @RFC- as planned, don't forget to check off tasks from the list after completeing said task and also update the implementation notes summary section with a summary of changes made, files impacted, links etc.. as this will be used as context by other llms and agents to understand the changes made when implementing an rfc before proceeding to next step/task. Also note to use the @Makefile cmds for running any cmds to run, test etc.. the app. need to make sure app fits into the make cmd interface created from earlier RFC so make sure to always use the makefile cmds for running builds tests lint serve etc... don't forget to check off tasks from the list after completeing said task and also update the implementation notes summary section with a summary of changes made, files impacted, links etc.. as this will be used as context by other llms and agents to understand the changes made when implementing an rfc before proceeding to next step/task. Also note to use the @Makefile cmds for running any cmds to run, test etc.. the app. need to make sure app fits into the make cmd interface created from earlier RFC so make sure to always use the makefile cmds for running builds tests lint serve etc...  don't forget to follow rules in .make sure tests pass before moving to next task. never move to next task until tests are passing start by running all tests and understanding the currnent state of project, schemas, and api before proceeding to continuing the next task. make syre to add your tests for each task before marking the task as completre in the rfc. Also if you have test failures, feel free to add debug logs and run tests in verbose mode so you can see a step by step log of how the program executes to investigate what might be wrong. If the test logs are so long and cut off, you can also pipe the logs to a debug.log file that gets rewritten everytime so you can see full logs.


##### VERSION 3##########
Pls go ahead and start implementing the rfc. Keep track of each task item and sub task for each task item in a todo list and make sure to check of in the rfc doc and update the implemntation summary detail section after complteing each task list. Before you attempt implementing each task list, make sure you research and study the project before implementing a solution to ensure solution integrates perfectly and correctly. 




[--------------------------------------------------------]

go ahead and start implementing the rfc as planned, don't forget to check off tasks from the list after completeing said task and also update the implementation notes summary section with a summary of changes made, files impacted, links etc.. as this will be used as context by other llms and agents to understand the changes made when implementing an rfc before proceeding to next step/task. Also note to use the @Makefile cmds for running any cmds to run, test, lint, build etc.. make sure to always use the makefile cmds for running builds tests lint serve etc...don't forget to check off tasks from the list after completeing said task and also update the implementation notes summary section with a summary of changes made, files impacted, links etc.. as this will be used as context by other llms and agents to understand the changes made when implementing an rfc before proceeding to next step/task. don't forget to follow rules in .make sure tests pass before moving to next task. never move to next task until tests are passing start by running all tests and understanding the currnent state of project, schemas, and api before proceeding to continuing the next task. make syre to add your tests for each task before marking the task as completre in the rfc. Also if you have test failures, feel free to add debug logs and run tests in verbose mode so you can see a step by step log of how the program executes to investigate what might be wrong. If the test logs are so long and cut off, you can also pipe the logs to a debug.log file that gets rewritten everytime so you can see full logs. Always make sure to use context7 mcp when checking library or tools documentation or guidelines so you're using what is most popular and upto date





[--------------------------------------------------------]

go ahead and start implementing the rfc as planned, don't forget to check off tasks from the list after completeing said task and also update the implementation notes summary section with a summary of changes made, files impacted, links etc.. as this will be used as context by other llms and agents to understand the changes made when implementing an rfc before proceeding to next step/task. Also note to use the @Makefile cmds for running any cmds to run, test, lint, build etc.. the app. need to make sure app fits into the make cmd interface created from earlier RFC so make sure to always use the makefile cmds for running builds tests lint serve etc... don't forget to check off tasks from the list after completeing said task and also update the implementation notes summary section with a summary of changes made, files impacted, links etc.. as this will be used as context by other llms and agents to understand the changes made when implementing an rfc before proceeding to next step/task. don't forget to follow rules in .make sure tests pass before moving to next task. never move to next task until tests are passing start by running all tests and understanding the currnent state of project, schemas, and api before proceeding to continuing the next task. make syre to add your tests for each task before marking the task as completre in the rfc. Also if you have test failures, feel free to add debug logs and run tests in verbose mode so you can see a step by step log of how the program executes to investigate what might be wrong. If the test logs are so long and cut off, you can also pipe the logs to a debug.log file that gets rewritten everytime so you can see full logs.